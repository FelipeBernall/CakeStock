<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <EditText
        android:id="@+id/editTempoPreparo"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:hint="Tempo de Preparo (minutos)"
        android:inputType="number" />

    <EditText
        android:id="@+id/editRendimento"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_below="@id/editTempoPreparo"
        android:hint="Rendimento (porções)" />

    <EditText
        android:id="@+id/editModoPreparo"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_below="@id/editRendimento"
        android:hint="Modo de Preparo (opcional)"
        android:inputType="textMultiLine" />

    <Button
        android:id="@+id/btnCadastrarReceita"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/editModoPreparo"
        android:text="Salvar" />
</RelativeLayout>
